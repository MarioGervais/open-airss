services:
  open-airss:
    image: ghcr.io/your-username/open-airss:latest
    # build: .   # uncomment to build locally instead
    ports:
      - "3000:3000"
    environment:
      PUBLIC_API_URL: ${PUBLIC_API_URL}
    restart: unless-stopped
